name: TEST_CONTEXT

# on:
#   push:
#     branches:
#     - main
#     - release/*
#   workflow_dispatch:


on:[workflow_dispatch]

    

jobs:
  build:
    runs-on: ubuntu-latest
    # needs: [CONDUCTOR_BLOBBERS_01_TESTS]
    steps:
      - name: Test step
        run: echo "Test step"